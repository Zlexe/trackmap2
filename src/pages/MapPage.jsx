import RailwayMap from '../components/index';

const MapPage = ({ mapData, onMapDataChange }) => {
  return <RailwayMap mapData={mapData} onMapDataChange={onMapDataChange} />;
};

export default MapPage;
